{
  "name": "tere-gram-bot",
  "version": "0.0.2",
  "description": "Simple telegram node bot with a few funny commands",
  "scripts": {
    "start": "micro-bot -d ${NOW_URL}",
    "pre-deploy": "node deleteLastDeploy.js",
    "deploy": "npm run pre-deploy && now --dotenv --public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.16.2",
    "cool-ascii-faces": "^1.3.4",
    "dotenv": "^4.0.0",
    "micro-bot": "^1.3.2"
  }
}
