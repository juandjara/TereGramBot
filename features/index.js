'use strict';

const cat = require('./cat');
const dog = require('./dog');
const donger = require('./donger');
const hiddenMessage = require('./hidden-message');
const messageListener = require('./message-listener');

module.exports = {
  cat,
  dog,
  donger,
  hiddenMessage,
  messageListener
};
